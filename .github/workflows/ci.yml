name: Continuous Integration
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: write-all
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # This step is correct: v2 with the token
      - uses: iterative/setup-cml@v2
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # We are keeping 3.11

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      # We are keeping this step, as it's needed for C-extensions
      - name: Install system build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3-dev cargo

      # --- THIS IS THE FIX ---
      - name: Install Packages
        run: |
          pip install --upgrade pip
          
          # 1. Install numpy BY ITSELF first.
          #    This will grab the pre-built wheel for Python 3.11
          pip install numpy
          
          # 2. Now install the rest of your requirements.
          #    pip will see numpy is already installed and skip it.
          pip install -r requirements.txt

      - name: Format
        run: make format

      - name: Train
        run: make train

      - name: Evaluation
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: make eval

      - name: Update Branch
        env:
          NAME: ${{ secrets.ADI }}
          EMAIL: ${{ secrets.ADI_MAIL }}
        run: make update-branch ADI=$NAME ADI_MAIL=$EMAIL
